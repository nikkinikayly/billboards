<%= @song.id ? "Edit Song" : "Create Song" %>

<%= form_for [@artist, @song] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :genre %>
  <%= f.text_field :genre %>
  <%= f.label :album %>
  <%= f.text_field :album %>
  <%= f.submit %>
<% end %>